/*
1. Crea un algoritmo que invierta un array de enteros.


*/
const numeros = [1, 2, [3, 4, 5], 6, [7, 8]];
function invertir(numeros) {
    for($i=numeros.length;$i>=0;$i--){
return $i
    }
    
}
let resultado=numeros.map(invertir);
